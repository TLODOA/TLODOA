# Run project
## Windows
```bash
python -m venv .venv
source .venv\Scripts\activate.bat
pip install -r requirements.txt
python app.py
```

## Linux
```cmd
python3 -m venv .venv
.venv/bin/activate
pip install -r requirements.txt
python3 app.py
```
